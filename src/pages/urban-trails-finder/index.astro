<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Urban Trails Finder</title>
    <meta
      name="description"
      content="A Stadtwanderweg-inspired interactive web map prototype for curated urban walking loops in US cities."
    >
    <link
      rel="stylesheet"
      href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css"
      integrity="sha256-p4NxAoJBhIINfQmMdI0Z4h12p8mX0hgGcR31xQp8ROA="
      crossorigin=""
    >
    <link rel="stylesheet" href="/urban-trails-finder/styles.css">
  </head>
  <body>
    <main class="app-shell">
      <aside class="sidebar" aria-label="Urban trail controls">
        <header class="app-header">
          <div>
            <p class="eyebrow">Stadtwanderweg-inspired GIS</p>
            <h1>Urban Trails Finder</h1>
          </div>
          <button class="icon-button" id="fitAllButton" type="button" title="Fit all trails" aria-label="Fit all trails">
            <i data-lucide="scan"></i>
          </button>
        </header>

        <section class="filter-panel" aria-label="Trail filters">
          <label for="cityFilter">City</label>
          <select id="cityFilter">
            <option value="all">All cities</option>
          </select>

          <label for="scoreFilter">Minimum trail score</label>
          <div class="range-row">
            <input id="scoreFilter" type="range" min="60" max="95" value="60">
            <output id="scoreValue" for="scoreFilter">60</output>
          </div>
          <button id="goCityButton" type="button" class="primary-action go-city-action">
            <i data-lucide="map-pinned"></i>
            Go
          </button>
        </section>

        <section class="summary-grid" aria-label="Filtered trail summary">
          <div>
            <span id="trailCount">0</span>
            <small>routes</small>
          </div>
          <div>
            <span id="cityCount">0</span>
            <small>cities</small>
          </div>
          <div>
            <span id="avgScore">0</span>
            <small>avg score</small>
          </div>
        </section>

        <section class="selected-panel" aria-live="polite">
          <div class="selected-heading">
            <div>
              <p id="selectedCity" class="eyebrow">Select a route</p>
              <h2 id="selectedName">Route details</h2>
            </div>
            <span id="selectedScore" class="score-pill">--</span>
          </div>
          <dl class="route-stats">
            <div>
              <dt>Distance</dt>
              <dd id="selectedDistance">--</dd>
            </div>
            <div>
              <dt>Transit</dt>
              <dd id="selectedTransit">--</dd>
            </div>
            <div>
              <dt>Comfort</dt>
              <dd id="selectedComfort">--</dd>
            </div>
          </dl>
          <p id="selectedDescription" class="selected-copy">
            Choose a trail from the list to inspect its path and export the geometry.
          </p>
          <div id="metricBars" class="metric-bars" aria-label="Route scoring metrics"></div>
          <div class="action-row">
            <button id="downloadGpx" type="button" class="primary-action" disabled>
              <i data-lucide="download"></i>
              GPX
            </button>
            <button id="downloadKml" type="button" class="secondary-action" disabled>
              <i data-lucide="file-down"></i>
              KML
            </button>
          </div>
        </section>

        <section class="trail-list-section" aria-label="Trail results">
          <div class="list-heading">
            <h2>City Wander Paths</h2>
            <div class="list-actions" aria-label="Export visible trails">
              <button id="downloadAllGpx" type="button" class="text-button">
                <i data-lucide="archive"></i>
                GPX
              </button>
              <button id="downloadAllKml" type="button" class="text-button">
                <i data-lucide="file-archive"></i>
                KML
              </button>
            </div>
          </div>
          <div id="trailList" class="trail-list"></div>
        </section>

        <footer class="method-note">
          <strong>Portfolio note:</strong> routes are generated from control points with Valhalla pedestrian routing over OpenStreetMap. A production version would add municipal sidewalk inventories, surface quality, slope, crossings, and transit stop joins.
        </footer>
      </aside>

      <section class="map-panel" aria-label="Interactive trail map">
        <div id="map"></div>
      </section>
    </main>

    <script
      is:inline
      src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"
      integrity="sha256-20nQCchB9co0qIjJZRGuk2/Z9VM+kNiyxNV1lvTlZBo="
      crossorigin=""
    ></script>
    <script is:inline src="https://unpkg.com/lucide@latest/dist/umd/lucide.min.js"></script>
    <script is:inline src="/urban-trails-finder/data.js"></script>
    <script is:inline src="/urban-trails-finder/app.js"></script>
  </body>
</html>
